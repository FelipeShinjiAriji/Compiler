%option noyywrap

%%

[a-zA-Z]([a-zA-Z]|[0-9])*   {printf("%s\n", yytext);}
"+"                         {printf("%s\n", yytext);}
"-"                         {printf("%s\n", yytext);}
[0-9]*[+-]?[0-9]*           {printf("%s\n", yytext);}
\n                          {;}
\r                          {;}
.                           {printf("ERRO\n");}

%%

int main (int argc , char ** argv)
{
    yylex();
    return 0;
}